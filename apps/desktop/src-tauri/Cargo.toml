[package]
name = "orca"
version = "0.1.12"
description = "A Tauri App"
authors = ["Andy Coupe"]
license = "MIT"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "orca_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
tokio = { version = "1", features = ["full"] }
tauri-plugin-dialog = "2"
tauri-plugin-notification = "2"
directories = "5"
ulid = "1"
thiserror = "1"
which = "6"
tokio-util = "0.7"
git2 = { version = "0.19", default-features = false }
handlebars = "5"
sha2 = "0.10"
# `default-fancy` skips the C-based onig regex (heavy to build) and ships the
# bundled syntaxes/themes — no external files at runtime.
syntect = { version = "5", default-features = false, features = ["default-fancy", "html"] }
# `two-face` bundles a curated extended SyntaxSet on top of syntect's defaults —
# adds TypeScript/TSX, Vue, Svelte, and many others that the upstream defaults
# don't ship. We use it instead of `SyntaxSet::load_defaults_newlines()`.
two-face = { version = "0.4", default-features = false, features = ["syntect-fancy"] }
tauri-plugin-updater = "2.10.1"
tauri-plugin-process = "2.3.1"
keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
portable-pty = "0.8"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
